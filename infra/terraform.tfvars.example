# Copy to terraform.tfvars and fill in your values
project_id  = "your-gcp-project-id"
region      = "us-east1"
bucket_name = "sondernista-site"

# Set domain for HTTPS + CDN + load balancer setup
# Leave empty ("") for plain GCS hosting
# domain = "sondernista.com"
