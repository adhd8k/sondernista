const photos=[...document.querySelectorAll('.photo')];const dialog=document.querySelector('dialog');const full=document.querySelector('#full-image');let current=0;let origin;
function show(i){current=(i+photos.length)%photos.length;const img=photos[current].querySelector('img');full.src=img.src;full.alt=img.alt;document.querySelector('#viewer-caption').textContent=img.alt;document.querySelector('#position').textContent=`${current+1} / ${photos.length}`;}
photos.forEach((p,i)=>p.addEventListener('click',()=>{origin=p;show(i);dialog.showModal();document.body.style.overflow='hidden';}));
document.querySelector('#close').onclick=()=>dialog.close();document.querySelector('#prev').onclick=()=>show(current-1);document.querySelector('#next').onclick=()=>show(current+1);dialog.addEventListener('close',()=>{document.body.style.overflow='';origin?.focus();});dialog.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();show(current-1);}if(e.key==='ArrowRight'){e.preventDefault();show(current+1);}});let startX=0;full.addEventListener('touchstart',e=>{startX=e.changedTouches[0].clientX;},{passive:true});full.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-startX;if(Math.abs(dx)>60)show(current+(dx<0?1:-1));},{passive:true});
